from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ML Solution to predict the future failure rates of unscheduled maintenance components and prevent them from failing again.',
    author='Abhishek Singh',
    license='MIT',
)
